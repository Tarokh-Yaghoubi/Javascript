
/**
 * 
 * 
 *      More of data types
 * 
 */

console.log(Infinity);
console.log(typeof Infinity);

let name = "tarokh";
console.log(`The name of our programmer is : ${name}`);

/**
 * 
 *      In some languages we have the char type, like in the C programming language 
 *      or in Java. But in JS, we do not have this type, we only have strings.
 * 
 */

let isGreater = 4 > 3;
console.log(isGreater);     // true 


// If a value is declared but not defined, its value is undefined

let somethingUndefined;     // now, this is undefined
let somethingUndefinedDirectly = undefined;     // this is also undefined

console.log(somethingUndefined, somethingUndefinedDirectly);

console.log(typeof alert);
console.log(typeof null);
console.log(typeof undefined);

